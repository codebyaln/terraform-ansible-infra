- hosts: workers
  become: yes
  tasks:

    - name: Get join command from master node
      set_fact:
        join_cmd: "{{ hostvars[groups['master'][0]].worker_join_cmd }}"

    - name: Join worker to Kubernetes cluster
      command: "{{ join_cmd }} --ignore-preflight-errors=all"
      args:
        warn: false
